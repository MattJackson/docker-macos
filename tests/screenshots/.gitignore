# Ignore timestamped per-run captures; only commit the reference images
# and their metadata.
# Format: YYYY-MM-DD_HHMMSS-{clear-color,login,desktop-idle}.png
*.png
!reference/
!reference/*.png

# Diff images produced by diff-reference.sh on failed runs — kept on disk
# for inspection but never committed.
diffs/
